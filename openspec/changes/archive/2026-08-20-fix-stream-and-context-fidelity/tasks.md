# 修复流式保真与上下文压缩失真 — 任务清单

## 1. Compactor 压缩改造

- [x] 1.1 压缩摘要消息改用 `role: 'user'`,内容以 `[上下文压缩]` 前缀开头
- [x] 1.2 摘要包含旧对话的可观测要点(从旧消息中提取用户请求与 assistant 结论摘录)
- [x] 1.3 保留"最近 N 条 + 前置一条压缩摘要"的结构
- [x] 1.4 确认压缩产物能进入 LLM 上下文(不被 convertToLlm 过滤)

## 2. 流式解析不丢段

- [x] 2.1 修改 openai 兼容流式转换:同一 chunk 同时含 content 与 tool_calls 时两者都消费
- [x] 2.2 文本增量与工具增量各自正确发射,不因先取 content 丢弃工具
- [x] 2.3 保持纯文本/纯工具 chunk 的既有单事件语义

## 3. 测试与验收

- [x] 3.1 单测:超过阈值时压缩摘要包含旧内容要点(非固定占位文案)
- [x] 3.2 单测:压缩摘要可进入后续上下文(role 不被过滤为纯 UI)
- [x] 3.3 单测:未超阈值不压缩,消息列表不变
- [x] 3.4 单测:同一 chunk 含文本+工具时,两种增量都不丢
- [x] 3.5 全量跑 `npm test`,377/377 通过（含新增 6 例），无回归

## 4. 收尾

- [x] 4.1 `openspec validate` 通过
- [ ] 4.2 归档 change,delta 合入主规格