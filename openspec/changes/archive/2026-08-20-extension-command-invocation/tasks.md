# 扩展命令调用入口 — 任务清单

## 1. 命令绑定与查询

- [x] 1.1 在命令系统暴露扩展命令查询器,供 executeCommand 按名查找扩展命令
- [x] 1.2 让 ExtensionAPI 已注册的命令能被命令系统枚举与查询

## 2. 交互分发接入

- [x] 2.1 在 executeCommand 的 default 分支,先按首词查询扩展命令;命中则执行
- [x] 2.2 触发形式支持两种:斜杠前缀 `/命令名 args` 与首词精确匹配 `命令名 args`
- [x] 2.3 参数以空白切分,首词之后的全部内容作为参数数组传给 execute
- [x] 2.4 execute 抛错时捕获并呈现错误信息,交互循环不中断
- [x] 2.5 普通（未命中内置命令、也未命中扩展命令）输入继续走既有 user 消息路径,行为零回归

## 3. 枚举可见入口

- [x] 3.1 扩展命令并入 `/help` 帮助列表（`extensionHelpLines`）
- [x] 3.2 该入口显示命令名与描述,条目来自扩展注册

## 4. 测试与验收

- [x] 4.1 单测:斜杠触发扩展命令,参数正确透传,结果可观察
- [x] 4.2 单测:首词精确触发扩展命令,参数正确透传
- [x] 4.3 单测:无参调用扩展命令正常处理
- [x] 4.4 单测:未注册输入不进入命令分支，普通对话路径零回归
- [x] 4.5 单测:命令 execute 抛错时错误被捕获并呈现
- [x] 4.6 全量跑 `npm test`，371 通过，无回归

## 5. 收尾

- [x] 5.1 将扩展命令枚举入口并入帮助列表
- [ ] 5.2 `openspec validate` 通过后走 archive 归档